/** Grid class for a field's item list: as many columns of at least 340px as fit. */
export const ITEM_LIST_CLASS =
  "list-none p-0 m-0 grid grid-cols-[repeat(auto-fit,minmax(min(100%,340px),1fr))] gap-x-8 gap-y-2";
