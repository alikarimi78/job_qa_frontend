/** Small white spinner that sits inside a button while its request runs. */
export default function Spinner() {
  return <span className="w-4 h-4 rounded-full border-2 border-white/40 border-t-white animate-spin" />;
}
